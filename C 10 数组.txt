数组 array
    数组是按顺序存储的一些类型相同的值, 在内存中是连续存储的

    整个数组有一个数组名, 通过整数下标访问数组中单独的元素(element)

    考虑到影响执行的速度, C编译器不会检查数组的下标是否正确.
    因此, 如果下标值超过数组容量, 会导致数据被放置在已被其他数据占用的地方, 可能会破坏程序的结果甚至导致程序异常中断

    识别数组元素的数字被称为下标(subscript), 索引(indice)或偏移量(offset)
    下标必须是整数, 而且要从0开始计数